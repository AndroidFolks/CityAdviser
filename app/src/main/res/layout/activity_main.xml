<?xml version="1.0" encoding="utf-8"?>
<RelativeLayout android:layout_width="match_parent"
    android:layout_height="match_parent"
    xmlns:android="http://schemas.android.com/apk/res/android">

    <Spinner
        android:id="@+id/spinnerSelector"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:layout_centerHorizontal="true"
        android:layout_marginTop="100dp"
        android:entries="@array/provinces"
        android:outlineSpotShadowColor="@color/colorPrimaryDark"/>

    <Button
        android:id="@+id/btnCities"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:text="@string/btnText"
        android:onClick="onClickBtnCities"
        android:layout_below="@id/spinnerSelector"
        android:layout_alignLeft="@id/spinnerSelector"
        android:layout_marginTop="30dp"/>

    <TextView
        android:id="@+id/txtDisplay"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:layout_below="@id/btnCities"
        android:layout_alignLeft="@id/btnCities"
        android:layout_marginTop="30dp"/>

</RelativeLayout>